# Architecture

- Backend lives in `/backend` as a Spring Boot API.
- Frontend will live in `/frontend` as a React client.
- Database planning lives in `/database` and `/docs`.
- Domain packages are grouped by feature area.
- Shared backend concerns live under `common`.
- API, persistence, and UI details will be designed incrementally.
